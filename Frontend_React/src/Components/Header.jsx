import React from "react";
import "./Header.css"; // Separate CSS file for styling

const Header = () => {
  return (
    <header className="app-header">
      <img src="/picture1.jpg" alt="Company Logo" className="company-logo" />
    </header>
  );
};

export default Header;

